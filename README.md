# case-study-subway-accessibility

Thin portfolio wrapper around the
[`subway-access` v0.5.1](https://github.com/random-walks/subway-access)
CASESTUDY. The canonical analysis lives upstream; this repo adds
portfolio framing, a browsable gallery of the 15 committed figures,
and a cross-walk mapping the upstream sections to `factor-factory`
engine families.

This is the standalone home of the case study, extracted from the
`blaise-website` monorepo with full history in July 2026. The published
post lives at
[blaiseoss.com/posts/subway-accessibility-gaps](https://blaiseoss.com/posts/subway-accessibility-gaps).

## Five-bullet summary

- **Subject**: ADA accessibility of the NYC subway system, April 2026
  vintage — 493 stations, 157 ADA-accessible (31.8%), 2,317 tracts,
  8.5M resident study population.
- **Headline finding**: 4,717,140 New Yorkers (55.4%) live in tracts
  with no ADA-accessible station within an 800 m walk; Queens
  (1.75M gap population) and Staten Island (9% coverage) are the
  most underserved.
- **Methods mirrored from upstream**: OLS equity regression
  (*R*² = .202, *F*(3, 2313) = 108.83, *p* < .001), Moran's *I* for
  spatial clustering (*I* = .23 for gap score, *z* = 40.87), 7-period
  tract × year panel with sourced ADA upgrade dates for 101 / 157
  stations.
- **Reliability twist**: 49 of 157 ADA stations operated below 95%
  elevator uptime over the 12-month observation window; Manhattan's
  coverage drops from 77% nominal to 71% effective when uptime-weighted.
- **Status**: not recomputed here. Figures + upstream reference are
  the artifacts. Regeneration requires cloning the upstream repo —
  see `manuscripts/UPSTREAM_REFERENCE.md`.

## Layout

```
case-study-subway-accessibility/
├── jellycell.toml
├── pyproject.toml
├── README.md                         ← this file
├── notebooks/
│   ├── 01_context_and_pointer.py    ← framing + headline numbers
│   ├── 02_figures_gallery.py        ← 15 figures inlined as static images
│   └── 03_cross_walk.py             ← 18-row table: section ↔ engine ↔ portfolio topic
├── artifacts/
│   ├── cross_walk.json
│   ├── headline_numbers.json
│   └── figures/                      ← 15 PNGs, committed as regular git binaries
└── manuscripts/
    ├── MANUSCRIPT.md                 ← portfolio framing (source of the published post)
    ├── UPSTREAM_REFERENCE.md         ← regenerate-from-upstream recipe
    └── tearsheets/                   ← auto-generated by jellycell export
```

## Run it

From the repo root:

```bash
uv sync
for nb in notebooks/[0-9]*.py; do uv run jellycell run "$nb"; done
```

`jellycell.toml` pins the project name and artifact paths, so the
notebooks resolve their outputs from the repo root. Artifacts and
tearsheets are regenerated by the run — never hand-edit them.

## Pointer to the canonical work

- Upstream library:
  [`random-walks/subway-access`](https://github.com/random-walks/subway-access)
- Upstream CASESTUDY: `examples/accessibility-change-over-time/CASESTUDY.md`
  in that repo
- Upstream engine audit: Appendix D of the upstream CASESTUDY
- This case study's manuscript: `manuscripts/MANUSCRIPT.md`

**Author**: [Blaise Albis-Burdige](https://blaiseab.com). Creator of
`subway-access`.
